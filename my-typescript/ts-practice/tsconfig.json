{

  "compilerOptions": {
    "target": "es2016",                                  /* 컴파일된 코드가 어떤 환경에서 실행될 지 정의한다. 예를들어 화살표 함수를 사용하고 target 을 es5로 한다면 일반 function 키워드를 사용하는 함수로 변환을 해준다. (하지만 es6로 설정하면 화살표 함수를 그대로 유지해줌) */
    "module": "commonjs",                                /* 컴파일된 코드가 어떤 모듈 시스템을 사용할지 정의함. 예를 들어서 이 값을 common 으로 하면 export default Sample 을 하게 됐을 때 컴파일 된 코드에서는 exports.default = helloWorld 로 변환해 주지만 이 값을 es2015로 하면 export default Sample 을 그대로 유지하게 된다.*/
    "strict": true,                                      /* 모든 타입 체킹 옵션을 활성화 한다는 것을 의미한다. */
    "esModuleInterop": true,                             /* commonjs 모듈 형태로 이루어진 파일을 es2015 모듈 형태로 불러올 수 있게 해준다. */
    "outDir" : "./dist" /* 상위 디렉토리에 dist라는 폴더를 생산해 결과물을 올림  */
  }
}
